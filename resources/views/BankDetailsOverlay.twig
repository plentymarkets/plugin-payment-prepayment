{% set bankAccount = config("system.bank") %}

<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
        <span aria-hidden="true">x</span>
    </button>
    <h4 class="modal-title">{{ trans("PrePayment::PaymentMethod.bankDetails") }}</h4>
</div>

<div class="modal-body form-horizontal">
    <div class="row">
        <span class="col-xs-12 col-sm-12">
            <table class="table table-striped table-hover table-sm" border="0" cellpadding="0">
                <tbody>
                    <tr>
                        <td align="left">{{ trans("PrePayment::PaymentMethod.bankAccountOwner") }}</td>
                        <td align="right">{{ bankAccount["accountOwner"] }}</td>
                    </tr>
                    <tr>
                        <td align="left">{{ trans("PrePayment::PaymentMethod.bankName") }}</td>
                        <td align="right">{{ bankAccount["name"] }}</td>
                    </tr>
                    <tr>
                        <td align="left">{{ trans("PrePayment::PaymentMethod.bankIban") }}</td>
                        <td align="right">{{ bankAccount["iban"] }}</td>
                    </tr>
                    <tr>
                        <td align="left">{{ trans("PrePayment::PaymentMethod.bankSwift") }}</td>
                        <td align="right">{{ bankAccount["swift"] }}</td>
                    </tr>
                </tbody>
            </table>
        </span>
    </div>
</div>
<div class="modal-footer">
    <button type="button" class="btn btn-primary" data-dismiss="modal" aria-label="Close">
        <i class="fa fa-check" aria-hidden="true">
        </i>
        OK
    </button>
</div>
